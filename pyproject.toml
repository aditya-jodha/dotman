[project]
name = "dotman"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.13"
dependencies = [
    "typer>=0.26.4",
]

[tool.typer]
main = "src.dotman.cli:app"
